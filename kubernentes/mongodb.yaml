apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
  namespace: todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
        - name: mongo
          image: mongo
          ports:
            - containerPort: 27017
          volumeMounts:
            - mountPath: /data/db
              name: mongo-storage
      volumes:
        - name: mongo-storage
          emptyDir: {}  # مؤقت - يُفضل استبداله بـ PersistentVolume لاحقًا
---
apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: todo
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017

